/**
 * Global constants for the Mentivy application.
 */

export const APP_CONFIG = {
    // To respect the 512MB free tier limit on MongoDB Atlas, 
    // we use a single placeholder image URL instead of unique uploads.
    DEFAULT_IMAGE_PLACEHOLDER: 'https://placehold.co/600x400/png?text=Mentivy+Image',
    
    // You can add other global constants here
};
